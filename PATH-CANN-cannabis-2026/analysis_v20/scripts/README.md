After a successful v20 analysis run, build the formatted workbook with:

`node build_publication_tables.mjs /full/path/to/the/v20/run/output`

The CSV outputs are the analysis source of truth; the workbook is a formatted publication artifact.
